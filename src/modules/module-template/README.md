# Template Module

This is the standard architectural template for building ERP modules.

## Getting Started

1. Copy this folder and rename it to your target module name.
2. Register your navigation endpoints in `src/shared/config/navigation.config.ts`.
3. Set your module permissions, dependencies, and api endpoints inside `module.json`.
4. Build pages, hooks, components, and services following the coding guidelines in `docs/`.
